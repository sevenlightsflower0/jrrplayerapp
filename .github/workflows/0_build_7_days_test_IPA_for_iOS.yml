name: 0_build_7_days_test_IPA_for_iOS

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build (7-days free testing)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64

      - run: flutter clean
      - run: flutter pub get

      # --- NEW: Replace post_install block with correct version ---
      - name: Replace post_install block in Podfile
        run: |
          python3 << 'PYTHON'
          import re
          podfile_path = 'ios/Podfile'
          with open(podfile_path, 'r') as f:
              content = f.read()
          
          lines = content.split('\n')
          new_lines = []
          in_post_install = False
          indent_level = 0
          for line in lines:
              stripped = line.lstrip()
              if stripped.startswith('post_install') and not in_post_install:
                  in_post_install = True
                  indent_level = len(line) - len(stripped)
                  continue  # skip the opening line
              if in_post_install:
                  if stripped == 'end' or stripped.startswith('end'):
                      current_indent = len(line) - len(stripped)
                      if current_indent <= indent_level:
                          in_post_install = False
                          continue
                  # skip all lines inside the block
                  continue
              else:
                  new_lines.append(line)
          
          # Append the new correct post_install block
          new_lines.append('')
          new_lines.append('post_install do |installer|')
          new_lines.append('  # Keep existing Flutter settings for Pods')
          new_lines.append('  installer.pods_project.targets.each do |target|')
          new_lines.append('    flutter_additional_ios_build_settings(target)')
          new_lines.append('  end')
          new_lines.append('')
          new_lines.append('  # Force-set DEVELOPMENT_TEAM for all Pod targets')
          new_lines.append('  installer.pods_project.targets.each do |target|')
          new_lines.append('    target.build_configurations.each do |config|')
          new_lines.append('      config.build_settings["DEVELOPMENT_TEAM"] = "DUMMYTEAM"')
          new_lines.append('      config.build_settings["CODE_SIGNING_ALLOWED"] = "NO"')
          new_lines.append('    end')
          new_lines.append('  end')
          new_lines.append('end')
          
          with open(podfile_path, 'w') as f:
              f.write('\n'.join(new_lines))
          print("Podfile post_install replaced successfully")
          PYTHON

      # Update CocoaPods repo (with retry)
      - name: Update CocoaPods repository with retry
        working-directory: ios
        run: |
          for i in {1..3}; do
            echo "Attempt $i: Updating CocoaPods repository..."
            pod repo update && break
            sleep 5
          done

      # Pod install (with retry)
      - name: Install CocoaPods dependencies with retry
        working-directory: ios
        run: |
          for i in {1..5}; do
            echo "Attempt $i: Running pod install..."
            pod install --repo-update && break
            echo "Retry in 10s..."
            sleep 10
          done

      # Set dummy team in main project (extra safety)
      - name: Set dummy Development Team in Xcode project
        run: |
          PROJECT_FILE="ios/Runner.xcodeproj/project.pbxproj"
          sed -i '' 's/DEVELOPMENT_TEAM = .*/DEVELOPMENT_TEAM = DUMMYTEAM;/g' "$PROJECT_FILE"
          sed -i '' 's/CODE_SIGN_IDENTITY = .*/CODE_SIGN_IDENTITY = "";/g' "$PROJECT_FILE"

      # Build
      - name: Build iOS (unsigned)
        env:
          CODE_SIGNING_ALLOWED: "NO"
          CODE_SIGN_IDENTITY: ""
        run: flutter build ios --release --no-codesign

      - name: Create Payload and IPA
        working-directory: build/ios/iphoneos
        run: |
          mkdir Payload
          mv Runner.app Payload/
          zip -qq -r -9 ../FlutterIpaExport.ipa Payload/
          ls -la ../FlutterIpaExport.ipa

      - name: Check IPA size
        run: |
          if [ -f "build/ios/FlutterIpaExport.ipa" ]; then
            SIZE=$(stat -f%z "build/ios/FlutterIpaExport.ipa")
            echo "IPA file size: $((SIZE / 1024 / 1024)) MB"
          else
            echo "ERROR: IPA file not found!"
            exit 1
          fi

      - name: Update outdated packages (optional)
        run: flutter pub outdated

      - name: Upload IPA as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: FlutterIpaExport.ipa
          path: build/ios/FlutterIpaExport.ipa
          retention-days: 7
        with:
          name: FlutterIpaExport.ipa
          path: build/ios/FlutterIpaExport.ipa
          retention-days: 7
